apiVersion: v1
kind: Service
metadata:
  name: messaging-app-service
spec:
  selector:
    app: messaging-app-blue  # Change this to "messaging-app-green" to switch traffic
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
